<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <!-- alongside your other .donor.* activities -->

    <uses-feature android:name="android.hardware.camera.any" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Food_saver">

        <!-- App now starts on SplashActivity, which then routes to LoginActivity -->
        <activity
            android:name=".auth.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".auth.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".auth.SignUpActivity"
            android:exported="false" />

        <activity
            android:name=".auth.CompleteGoogleProfileActivity"
            android:exported="false" />

        <activity
            android:name=".auth.PendingVerificationActivity"
            android:exported="false" />

        <activity android:name=".donor.PostFoodActivity" android:exported="false" />
        <activity android:name=".donor.DonorHomeActivity" android:exported="false" />
        <activity android:name=".donor.MyDonationsActivity" android:exported="false" />
        <activity android:name=".donor.NgoRequestsActivity" android:exported="false" />
        <activity
            android:name=".donor.ChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".donor.ProfileActivity" android:exported="false" />

        <!-- MainActivity kept, but no longer the launcher. Repurpose it later
             (e.g. as the Donor/NGO home shell) or delete it if unused. -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- alongside your other .donor.* activities -->
        <activity android:name=".donor.EditFoodPostActivity" android:exported="false" />

    </application>

</manifest>